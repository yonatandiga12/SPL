package bgu.spl.mics.application.objects;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import static bgu.spl.mics.application.objects.GPU.Type.*;
import static org.junit.Assert.*;

public class GPUTest {

    //GPU g;

    @Before
    public void setUp() throws Exception {

        //Model m = new Model(1, new Data(), new Student());
        //g = new GPU(RTX3090, m);
    }


    @Test
    public void prepareData()
    {
        //g.prepareData();
        //assertEquals(g.getDataSize()/1000,g.getDataBatchSize());

    }

    @Test
    public  void trainModel()
    {
        /*
        int x= g.trainModel(new Data("Images", 3));

        if(g.getType()=="RTX3090")
        {
            assertEquals(4,x);
        }
        if(g.getType()=="RTX2080")
        {
            assertEquals(2,x);
        }
        if(g.getType()=="GTX1080")
        {
            assertEquals(1,x);
        }

         */


    }


}